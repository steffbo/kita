backend:
  name: git-gateway
  repo: steffbo/kita
  branch: main

media_folder: 'assets/uploads'
public_folder: '/assets/uploads'

locale: "de"

collections:
  - name: 'index'
    label: 'Home'
    delete: false
    editor:
      preview: false
    files:
      - name: 'index'
        label: 'Home'
        file: 'index.md'
        fields:
          - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'page' }
          - { label: 'Title', name: 'title', widget: 'string' }
          - { label: 'Inhalt', name: 'body', widget: 'markdown' }
  - name: 'pages'
    label: 'Pages'
    folder: '_pages/'
    create: true
    slug: '{{slug}}'
    editor:
      preview: false
    fields:
      - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'page' }
      - { label: 'Titel', name: 'title', widget: 'string' }
      - { label: 'Link', name: 'permalink', hint: 'Unter diesem Pfad kann die Seite direkt aufgerufen werden', widget: 'string' }
      - { label: 'Badge', name: 'show-badge', hint: 'Zeigt einen roten Punkt an', widget: 'boolean' }
      - { label: 'Reihenfolge im Menu', name: 'menu-order', hint: 'Die Einträge im Menu können hiermit sortiert werden. Negative Werte können benutzt werden um die Seite nicht im Menu erscheinen zu lassen.', widget: 'number' }
      - { label: 'Inhalt', name: 'body', widget: 'markdown' }
  - name: 'blog'
    label: 'Blog'
    folder: '_posts'
    create: true
    slug: '{{year}}-{{month}}-{{day}}-{{slug}}'
    editor:
      preview: false
    fields:
      - {label: 'Layout', name: 'layout', widget: 'hidden', default: 'post'}
      - {label: 'Titel', name: 'title', widget: 'string'}
      - {label: 'Veröffentlichungsdatum', name: 'date', widget: 'datetime'}
      - {label: 'Inhalt', name: 'body', widget: 'markdown'}

  - name: 'sponsors'
    label: 'Sponsoren'
    delete: false
    editor:
      preview: false
    files:
      - name: 'sponsors'
        label: 'Sponsoren'
        file: '_data/sponsors.yml'
        fields:
          - label: 'Sponsoren'
            name: 'sponsors'
            widget: 'list'
            fields:
              - { label: 'Name', name: 'name', widget: 'string', hint: 'Name des Sponsors' }
              - { label: 'Logo', name: 'logo', widget: 'image', required: false, hint: 'Logo des Sponsors (empfohlen: max. 500x200px)', media_folder: '/assets/img/sponsors', public_folder: '/assets/img/sponsors' }
              - { label: 'Beschreibung', name: 'description', widget: 'text', required: false, hint: 'Kurze Beschreibung des Sponsors (1-2 Sätze)' }
              - { label: 'Website', name: 'website', widget: 'string', required: false, hint: 'URL zur Website des Sponsors (z.B. https://www.beispiel.de)' }
              - { label: 'Unterstützung', name: 'support', widget: 'text', required: false, hint: 'Wie hat der Sponsor die Kita unterstützt? (z.B. "Finanzierung des Spielplatzes 2024")' }

  - name: 'events'
    label: 'Kalender-Einträge'
    delete: false
    editor:
      preview: false
    files:
      - name: 'closures'
        label: 'Kalender: Schließzeiten'
        file: '_data/closures.yml'
        fields:
          - label: 'Einträge'
            name: 'events'
            widget: 'list'
            fields:
              - { label: 'Titel/Grund', name: 'label', widget: 'string' }
              - { label: 'Typ', name: 'type', widget: 'hidden', default: 'closure' }
              - { label: 'Start', name: 'start', widget: 'datetime', format: 'YYYY-MM-DD', time_format: false }
              - { label: 'Ende (optional)', name: 'end', widget: 'datetime', required: false, format: 'YYYY-MM-DD', time_format: false }
      - name: 'appointments'
        label: 'Kalender: Termine (Eltern)'
        file: '_data/events.yml'
        fields:
          - label: 'Einträge'
            name: 'events'
            widget: 'list'
            fields:
              - { label: 'Titel', name: 'label', widget: 'string' }
              - { label: 'Typ', name: 'type', widget: 'hidden', default: 'event' }
              - { label: 'Start', name: 'start', widget: 'datetime', format: 'YYYY-MM-DD', time_format: false }
              - { label: 'Ende (optional)', name: 'end', widget: 'datetime', required: false, format: 'YYYY-MM-DD', time_format: false }
      - name: 'festivities'
        label: 'Kalender: Feste'
        file: '_data/festivities.yml'
        fields:
          - label: 'Einträge'
            name: 'events'
            widget: 'list'
            fields:
              - { label: 'Titel', name: 'label', widget: 'string' }
              - { label: 'Typ', name: 'type', widget: 'hidden', default: 'festivity' }
              - { label: 'Start', name: 'start', widget: 'datetime', format: 'YYYY-MM-DD', time_format: false }
              - { label: 'Ende (optional)', name: 'end', widget: 'datetime', required: false, format: 'YYYY-MM-DD', time_format: false }
